const quarterValue = (num) => {
    const quarteredNum = num / 4;

    return quarteredNum;
}

// Invoke the function and store the return value
const result = quarterValue(4);

// Log the return value
console.log(result);